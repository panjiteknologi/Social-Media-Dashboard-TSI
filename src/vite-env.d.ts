/// <reference types="vite/client" />

interface ImportMetaEnv {
  /** Development only: `true` shows the design's sample data instead of not-connected states. */
  readonly VITE_SAMPLE_DATA?: string;
}
